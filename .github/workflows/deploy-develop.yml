name: Deploy — develop

on:
  push:
    branches: [develop]

jobs:
  deploy:
    name: Deploy to server
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      # ─── Backend ─────────────────────────────────────────────────
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install backend dependencies
        working-directory: backend
        run: pip install -r requirements.txt

      # ─── Frontend ────────────────────────────────────────────────
      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"
          cache-dependency-path: frontend/package-lock.json

      - name: Install frontend dependencies
        working-directory: frontend
        run: npm ci

      - name: Build frontend
        working-directory: frontend
        run: npm run build
        env:
          NEXT_PUBLIC_API_URL: ${{ secrets.NEXT_PUBLIC_API_URL }}

      # ─── Deploy (раскомментируй нужный вариант) ──────────────────

      # Вариант A: деплой на VPS через SSH
      # - name: Deploy via SSH
      #   uses: appleboy/ssh-action@v1
      #   with:
      #     host: ${{ secrets.SSH_HOST }}
      #     username: ${{ secrets.SSH_USER }}
      #     key: ${{ secrets.SSH_PRIVATE_KEY }}
      #     script: |
      #       cd /opt/avatar
      #       git pull origin develop
      #       cd backend && pip install -r requirements.txt
      #       sudo systemctl restart avatar-backend
      #       cd ../frontend && npm ci && npm run build
      #       sudo systemctl restart avatar-frontend

      # Вариант B: Railway
      # - name: Deploy to Railway
      #   uses: bervProject/railway-deploy@v1
      #   with:
      #     railway_token: ${{ secrets.RAILWAY_TOKEN }}
      #     service: avatar-backend

      # Вариант C: Render (webhook trigger)
      # - name: Deploy to Render
      #   run: curl -X POST ${{ secrets.RENDER_DEPLOY_HOOK_URL }}

      - name: Notify success
        run: echo "✅ Deploy triggered for develop branch"
